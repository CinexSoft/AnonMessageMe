{
  "rules": {
    "preview": {
      "$sid": {
        ".read": "auth != null && auth.uid == $sid",
        ".write": true
      }
    },
    "production": {
      "$sid": {
        ".read": "auth != null && auth.uid == $sid",
        ".write": true
      }
    }
  }
}